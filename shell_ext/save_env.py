"""
# TODO, b

create dcore script that saves tmux, bashrc, ... files somewhere. also enable over ssh.

    ~/.vim
    ~/.vimrc
    --> command to store, command to restore
    ~/.tmux
    ~/.profile (macos)
    ~/.bashrc
"""
